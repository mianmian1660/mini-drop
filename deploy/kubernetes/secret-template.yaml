apiVersion: v1
kind: Secret
metadata:
  name: mini-drop-secrets
  namespace: mini-drop
type: Opaque
stringData:
  # Replace every value before applying. Do not commit real production secrets.
  PG_DSN: "host=postgres user=postgres password=REPLACE_ME dbname=drop sslmode=require"
  POSTGRES_PASSWORD: "REPLACE_ME"
  MINIO_ROOT_USER: "REPLACE_ME"
  MINIO_ROOT_PASSWORD: "REPLACE_ME"
  S3_ACCESS_KEY: "REPLACE_ME"
  S3_SECRET_KEY: "REPLACE_ME"
